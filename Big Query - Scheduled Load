LOAD DATA INTO project.dataset.exchange_rates_raw (ingestion_timestamp TIMESTAMP, base_currency STRING, date DATE, rates JSON)
FROM FILES (
  format = 'JSON',
  uris = ['gs://<bucket-name>/raw/exchange_rates/*.json']
);
